# Environments
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Candidate pool is distributed by the organizers — never committed (465 MB)
data/
*.jsonl
*.jsonl.gz

# Large generated artifacts (regenerated by scripts/; small ones are committed)
artifacts/*.npy
artifacts/*.npz
# ...except the embedding cache: shipping it makes Stage-3 reproduction
# frictionless (71 MB, under GitHub's limit; regenerable by scripts/)
!artifacts/embeddings_cache.npz
artifacts/models/

# Local scratch
*.log
.DS_Store
Thumbs.db

# Generated submissions (kept out until the final one is blessed)
submission*.csv
